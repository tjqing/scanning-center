# 扫描中心改造待办

本文档根据项目分析中的主要问题 2～7 整理，覆盖规则快照、配置生效、异步任务可靠性、数据一致性、扫描性能与安全边界，以及前端工程化。

## 优先级说明

- **P0**：影响扫描结果正确性、任务可靠性或数据一致性，应优先完成。
- **P1**：影响生产稳定性、性能或安全边界，建议在上线前完成。
- **P2**：改善维护成本和用户体验，可在核心问题解决后推进。

## 一、任务规则快照真正参与执行（P0）

### 目标

保证任务创建后使用的规则内容保持不变，后续编辑规则不会改变历史任务的扫描行为和结果。

### 待办

- [ ] 定义稳定的规则快照结构，至少包含规则 ID、编码、名称、类型、扫描对象、风险等级、匹配方式、匹配内容、大小写配置、文件类型、排除模式、问题说明、整改建议和提示词。
- [ ] 创建任务时对快照字段做版本化，例如增加 `snapshotVersion`。
- [ ] 修改 `TaskMapper.findTaskRules`，从 `task_rule.rule_snapshot` 反序列化规则，不再关联读取 `scan_rule` 当前配置。
- [ ] 对损坏、缺失或不兼容的规则快照给出明确任务错误，不能静默回退到实时规则。
- [ ] 保留规则 ID 用于问题追溯，但问题展示不应依赖当前规则仍然存在。
- [ ] 在问题表中保存规则名称或规则编码快照，避免历史问题查询必须关联当前规则表。
- [ ] 增加测试：创建任务后修改规则，任务仍按创建时规则执行。
- [ ] 增加测试：规则停用后，已创建任务仍可按快照执行。
- [ ] 增加测试：非法快照会使任务失败并记录可读错误。

### 验收标准

- 同一任务在规则被修改前后执行，匹配逻辑和问题描述保持一致。
- 历史结果不依赖当前规则的名称、状态或配置。

## 二、让已保存的扫描配置真正生效（P0）

### 2.1 任务扫描范围 `scopeJson`

- [ ] 明确并记录 `scopeJson` 的 JSON Schema，例如分支、包含路径、排除路径和文件类型。
- [ ] 使用 DTO 替代前后端直接传递任意 JSON 字符串。
- [ ] 创建任务时校验范围字段、路径格式和非法路径片段。
- [ ] 执行器根据任务范围筛选文件，并定义任务范围与扫描源配置的合并规则。
- [ ] 前端增加扫描范围表单，不要求用户手写 JSON。
- [ ] 在任务详情中展示最终生效的扫描范围。

### 2.2 扫描路径 `scanPaths`

- [ ] 在文件发现阶段只遍历 `scanPaths` 指定的目录或文件。
- [ ] 校验所有扫描路径都位于任务工作区内，拒绝绝对路径和 `..` 越界路径。
- [ ] 明确空值语义：空值表示扫描整个资源目录。
- [ ] 对不存在的扫描路径给出告警或失败结果，不应静默忽略。

### 2.3 扫描对象 `targetType`

- [ ] 定义 CODE 和 DOCUMENT 对应的扩展名集合。
- [ ] 在规则匹配前同时校验 `targetType`、规则文件类型和扫描源文件类型。
- [ ] 对无法分类的扩展名制定明确策略，并补充测试。

### 2.4 AI 规则

- [ ] 在未接入 AI 网关前，前端明确标注“仅配置、暂不执行”。
- [ ] 创建任务时如选择 AI 规则，给出提示或禁止执行，避免任务成功但规则被静默跳过。
- [ ] 接入 AI 前定义模型适配器、分段策略、超时、重试、响应格式校验、数据脱敏和审计方案。

### 2.5 数据库文档源

- [ ] 明确产品选择：使用应用自身数据源，或支持独立数据库连接。
- [ ] 若仅使用应用数据源，删除无效的数据库 URL、用户名和密码字段及界面项。
- [ ] 若支持独立连接，实现独立只读 DataSource、连接超时、连接测试和凭据加密。
- [ ] 使用最低权限数据库账号，并对允许查询的库、表或视图设置白名单。

### 验收标准

- 创建任务时页面展示的范围与执行器实际扫描的文件集合一致。
- 系统中不存在“可以保存但完全不生效”且未明确标注的配置项。

## 三、异步任务可靠性（P0）

### 待办

- [ ] 将任务事件改为事务提交后触发，例如使用 `@TransactionalEventListener(AFTER_COMMIT)`。
- [ ] 处理线程池队列满、任务拒绝和事件监听异常，失败时回写任务状态和原因。
- [ ] 启动时扫描长时间停留在 `QUEUED`、`RUNNING` 或 `CANCEL_REQUESTED` 的任务并执行恢复策略。
- [ ] 为任务增加领取时间、心跳时间、执行实例和重试次数等字段。
- [ ] 使用带条件的原子更新领取任务，避免多实例重复执行。
- [ ] 定义最大执行时长、最大重试次数和人工重试入口。
- [ ] 将 `scan-center.worker-threads` 接入线程池配置，移除硬编码线程数。
- [ ] 增加线程池活跃数、队列深度、任务耗时和失败数监控。
- [ ] 明确取消语义：排队任务立即取消，运行任务在文件边界或可中断点退出。
- [ ] 增加应用重启、重复事件、并发领取、队列已满和执行异常测试。

### 推荐实施顺序

1. 事务提交后触发事件。
2. 启动恢复和超时任务补偿。
3. 数据库原子领取与幂等控制。
4. 独立 Worker 或消息队列；仅在任务规模需要时引入。

### 验收标准

- API 返回创建成功后，任务不会因为事务竞态永久停留在 `QUEUED`。
- 应用异常重启后，未完成任务可以恢复、重试或明确标记失败。
- 同一个任务不会被两个 Worker 同时执行。

## 四、数据一致性和生命周期管理（P0）

### 待办

- [ ] 删除待执行任务时同步删除 `task_rule` 等关联数据，或改为任务逻辑删除。
- [ ] 为核心关联增加外键或等效的数据库约束，并明确级联删除策略。
- [ ] 为 `task_file(task_id, relative_path)` 增加唯一约束，避免重复文件记录。
- [ ] 使用 UUID、数据库序列或雪花 ID 生成任务编号，避免毫秒时间戳并发冲突。
- [ ] 定义零文件任务状态：建议标记失败或增加 `NO_FILES` 业务错误。
- [ ] 替换 ZIP 成功后删除旧存储文件；数据库更新失败时清理新文件。
- [ ] 删除未被任务引用的上传扫描源时同步清理其存储文件。
- [ ] 增加定时存储清理任务，识别数据库未引用的孤儿文件和过期工作区。
- [ ] 为问题状态建立允许的转换规则，例如 `PENDING → CONFIRMED → RESOLVED`，忽略和重新打开需单独定义。
- [ ] 记录问题状态变更历史，包括原状态、新状态、处理人、说明和时间。
- [ ] 扫描结果和任务最终状态更新采用清晰的事务或补偿策略，避免统计值与问题明细不一致。
- [ ] 增加并发创建、删除任务、ZIP 替换失败、状态转换和汇总一致性测试。

### 验收标准

- 数据库不存在无主的任务规则、任务文件、结果或问题记录。
- 数据库记录与存储目录中的上传文件可以互相核对。
- 任务汇总数字能够由文件记录和问题明细重新计算得到相同结果。

## 五、扫描性能与安全边界（P1）

### 5.1 正则和匹配性能

- [ ] 对正则规则增加执行超时或使用支持安全限制的正则方案。
- [ ] 创建规则时识别并拒绝明显高风险的灾难性回溯表达式。
- [ ] 规则在任务开始时预编译一次，避免逐文件重复编译。
- [ ] 明确问题数量上限是“每个文件”还是“每个任务”，超过上限时记录截断信息。
- [ ] 问题入库改为批量写入，避免每次命中执行一次数据库插入。

### 5.2 文件解析限制

- [ ] 对文本、DOCX、PDF 分别设置可配置的单文件大小上限。
- [ ] 对 PDF 页数、DOCX 段落数、解析文本长度设置上限。
- [ ] 解析前识别二进制或异常编码文件，避免将任意二进制作为 UTF-8 文本读取。
- [ ] 将文件过大、格式损坏、加密 PDF 等情况记录为明确的文件失败原因。
- [ ] 保留现有 ZIP 路径、条目数量和解压总大小保护，并增加压缩比限制测试。

### 5.3 Git 来源限制

- [ ] 为 Git 克隆设置连接、传输和任务总超时。
- [ ] 限制允许的 URI 协议，按部署环境决定是否允许 SSH、本地路径和 `file://`。
- [ ] 对目标域名、IP 或仓库地址设置白名单，防止访问内部敏感网络。
- [ ] 设置最大仓库体积和最大文件数量，必要时支持浅克隆。
- [ ] 避免在日志、异常和命令输出中泄露访问令牌。

### 5.4 数据库查询和导出

- [ ] 不只依赖 `startsWith("select")` 判断安全性，改用只读账号、白名单视图和数据库权限控制。
- [ ] 为数据库文档读取设置内容总字节数上限，而不仅是行数和超时。
- [ ] Excel 导出改为流式工作簿或分页写出，限制最大导出量并提示截断。
- [ ] 对来自扫描内容的 Excel 单元格值处理公式注入风险。

### 5.5 可观测性

- [ ] 为资源准备、文件发现、文件解析、规则匹配、问题写入分别记录耗时。
- [ ] 增加任务级结构化日志，并统一携带 taskId、repositoryId 和文件路径。
- [ ] 增加扫描失败率、超时数、文件吞吐量、问题入库量和工作区磁盘占用监控。

### 验收标准

- 单个恶意正则、超大 PDF、超大仓库或异常数据库查询不能无限占用 Worker。
- 大量问题命中时数据库写入和 Excel 导出不会导致明显内存失控。

## 六、前端工程化改造（P2）

### 目录与模块

- [ ] 按规则、扫描源、任务、结果、问题、用户拆分 API 文件。
- [ ] 提取通用页面布局、搜索栏、分页表格、状态标签、详情网格和确认操作组件。
- [ ] 将大型 View 拆分为列表、编辑表单和详情组件。
- [ ] 格式化当前单行 Vue 文件，统一 ESLint、Prettier 和提交前检查。
- [ ] 删除未使用的 Vuex，或在确有跨页面状态需求时再建立 Store 模块。

### 表单和交互

- [ ] 使用 `el-form` rules 统一校验，保留后端校验作为最终边界。
- [ ] 抽取枚举字典，将英文状态显示为中文标签并统一颜色。
- [ ] 所有列表补齐 loading、空状态、失败提示和防重复提交。
- [ ] 操作成功后统一刷新策略，并处理接口失败时开关状态回滚。
- [ ] 任务页面展示自动刷新状态，避免页面销毁后仍有请求回写。
- [ ] 增加任务文件明细、失败原因和实际扫描范围展示。
- [ ] 对 AI 规则、数据库连接和暂未支持的功能提供清晰说明。

### API 与路由

- [ ] 统一请求错误模型，保留后端业务码、HTTP 状态和请求标识。
- [ ] 为列表筛选条件和分页状态提供 URL 持久化，刷新页面后仍能恢复。
- [ ] 在认证能力完成后增加路由守卫和按钮级权限控制。
- [ ] 将 API 基地址、环境名称和代理目标改为环境配置，避免写死端口。

### 测试

- [ ] 为公共校验函数和状态转换增加单元测试。
- [ ] 为创建扫描源、创建任务、取消任务和处理问题增加组件测试。
- [ ] 增加一条完整端到端流程：创建规则 → 上传 ZIP → 执行任务 → 查看问题 → 更新状态。

### 验收标准

- 页面组件职责清晰，业务 API、字典和通用交互不再重复实现。
- 核心扫描流程具备自动化前端测试，接口失败时页面状态能够正确恢复。

## 建议里程碑

### 里程碑 1：结果可信

- [ ] 完成规则快照执行。
- [ ] 完成扫描范围、扫描路径和扫描对象过滤。
- [ ] 修复任务关联数据和零文件状态。

### 里程碑 2：任务可靠

- [ ] 完成事务提交后触发、任务恢复和幂等领取。
- [ ] 完成任务超时、重试、取消和运行指标。
- [ ] 完成上传文件生命周期管理。

### 里程碑 3：生产稳定

- [ ] 完成正则、文件、Git、数据库查询和导出的资源限制。
- [ ] 完成批量问题写入和关键监控。
- [ ] 补齐后端集成测试和异常场景测试。

### 里程碑 4：前端可维护

- [ ] 完成页面拆分、统一表单校验和环境配置。
- [ ] 完成任务明细和扫描范围展示。
- [ ] 补齐组件测试与核心端到端测试。
