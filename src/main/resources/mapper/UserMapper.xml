<?xml version="1.0" encoding="UTF-8"?><!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.scan.center.mapper.UserMapper">
 <resultMap id="user" type="com.scan.center.model.SystemUser"><id column="id" property="id"/><result column="username" property="username"/><result column="external_user_id" property="externalUserId"/><result column="display_name" property="displayName"/><result column="application" property="application"/><result column="role_code" property="roleCode"/><result column="description" property="description"/><result column="enabled" property="enabled"/><result column="deleted" property="deleted"/><result column="create_time" property="createTime"/><result column="update_time" property="updateTime"/></resultMap>
 <sql id="cols">id,username,external_user_id,display_name,application,role_code,description,enabled,deleted,create_time,update_time</sql>
 <sql id="where">WHERE deleted=FALSE <if test="keyword!=null and keyword!=''">AND (username LIKE CONCAT('%',#{keyword},'%') OR display_name LIKE CONCAT('%',#{keyword},'%'))</if><if test="roleCode!=null and roleCode!=''">AND role_code=#{roleCode}</if><if test="enabled!=null">AND enabled=#{enabled}</if></sql>
 <select id="page" resultMap="user">SELECT <include refid="cols"/> FROM system_user <include refid="where"/> ORDER BY update_time DESC,id DESC LIMIT #{size} OFFSET #{offset}</select><select id="count" resultType="long">SELECT COUNT(*) FROM system_user <include refid="where"/></select><select id="findById" resultMap="user">SELECT <include refid="cols"/> FROM system_user WHERE id=#{id} AND deleted=FALSE</select><select id="countUsername" resultType="int">SELECT COUNT(*) FROM system_user WHERE deleted=FALSE AND LOWER(username)=LOWER(#{username}) <if test="excludeId!=null">AND id&lt;&gt;#{excludeId}</if></select>
 <insert id="insert" useGeneratedKeys="true" keyProperty="id">INSERT INTO system_user(username,external_user_id,display_name,application,role_code,description,enabled,deleted) VALUES(#{username},#{externalUserId},#{displayName},#{application},#{roleCode},#{description},#{enabled},FALSE)</insert><update id="update">UPDATE system_user SET username=#{username},external_user_id=#{externalUserId},display_name=#{displayName},application=#{application},role_code=#{roleCode},description=#{description},enabled=#{enabled},update_time=CURRENT_TIMESTAMP WHERE id=#{id} AND deleted=FALSE</update><update id="updateStatus">UPDATE system_user SET enabled=#{enabled},update_time=CURRENT_TIMESTAMP WHERE id=#{id} AND deleted=FALSE</update><update id="logicalDelete">UPDATE system_user SET deleted=TRUE,enabled=FALSE,update_time=CURRENT_TIMESTAMP WHERE id=#{id} AND deleted=FALSE</update>
 <delete id="deleteRepositoryRelations">DELETE FROM user_repository_relation WHERE user_id=#{userId}</delete>
 <insert id="insertRepositoryRelation">INSERT INTO user_repository_relation(user_id,repository_id,create_user_id,create_user_name) VALUES(#{userId},#{repositoryId},#{createUserId},#{createUserName})</insert>
 <select id="countRepositoryRelation" resultType="int">SELECT COUNT(*) FROM user_repository_relation WHERE user_id=#{userId} AND repository_id=#{repositoryId}</select>
</mapper>
